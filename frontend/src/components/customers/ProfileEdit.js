import { View } from "react-native"

const ProfileEdit = () => {
    // set up states here for fields, maybe we use an object state
    
    // update Function to call the backend

    return <View>
        {/* Input fields to edit profile */}
    </View>
}

export default ProfileEdit;