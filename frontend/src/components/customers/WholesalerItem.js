import { View } from "react-native"

const WholesalerItem = () => {
    // connect to maps api to find distance
    return <View>
        {/* Includes whole saler name, Location, distance away, stocks, price */}
    </View>
}

export default WholesalerItem